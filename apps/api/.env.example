# All environment variables live in the repo root:
#   ../../.env
#   ../../.env.local  (optional overrides, gitignored)
#
# The API loads the root .env via apps/api/src/loadEnv.ts (loadMonorepoEnv).
