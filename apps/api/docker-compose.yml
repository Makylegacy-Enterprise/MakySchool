services:
  makyschool-api:
    build: .
    container_name: makyschool-api
    restart: unless-stopped
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
